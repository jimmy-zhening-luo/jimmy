# The purpose of .env.example is to provide an example of a local test
# config that can be used to configure the local test environment.

# More detailed explanation TBD.

# To use, create a new file in your root directory named .env
# Then, copy and paste the contents of .env.example into .env

LOCAL_TEST_HOST="localhost"
LOCAL_TEST_PORT="8080"
