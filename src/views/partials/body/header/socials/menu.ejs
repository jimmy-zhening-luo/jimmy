<% accounts.forEach(account => { %>

    <%-
        include("./menu/button.ejs", {
            fill: {
                target: (account.site.baseUrl + account.user.handle),
                visual: account.site.title,
                class: "btn"
            }
        });
    %>

<% }); %>
